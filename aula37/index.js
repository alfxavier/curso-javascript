const nome = 'André Luis de Freitas Xavier'

for (let i of nome){
    console.log(i);
}

// For claśsico - Geralmente com iteráveis (array ou strings)
// For in - Retorna o índice ou chave (string, array ou objeto)
// For of - Retorna o valor em si (iteráveis, array ou strings)